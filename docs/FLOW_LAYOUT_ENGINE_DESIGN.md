# IQS-Flow 布局算法 v5（实现同步版）

> **状态**: 已在 flowToSVG / FlowDiagram 实现（与代码同步）
> **关键**: 流程绘制格 3×3（节点居中）+ X×Y 交叉格切分 + 整列/整行统一扩展 + 两趟端口分配 + 避障连线 + 表头格子化 + 高宽自适应（宽/高优先）

---

## ① 流程绘制格（3×3 切分）

一个"流程绘制格" = 一个流程节点，切成 3×3 九小格（W=节点宽 / H=节点高）：

```
第一行： 0.5W×0.5H │ 1W×0.5H │ 0.5W×0.5H   ← 连线区
第二行： 0.5W×1H   │ 1W×1H   │ 0.5W×1H     ← 中心格 = 节点
第三行： 0.5W×0.5H │ 1W×0.5H │ 0.5W×0.5H   ← 连线区
```

- **中心格（2,2）= 1W×1H** 放节点，节点**严格居中**（`cx = 绘制格左缘 + 绘制格宽/2`）
- **四周 8 格 = 连线区**，连线从连线区**中线**出发/抵达（距节点图形边缘 `half/2`），**首末段垂直于节点该边中点**
- **不填充连线区颜色**（绘制格周围无色块）

## ② 泳道交叉格 X×Y 切分

一个交叉格的绘制格数由格内节点 V/H 链排决定（**无标注默认纵向 V**）：

| 交叉格节点排布 | X×Y |
|:---|:---|
| 1 节点 | 1×1 |
| 2 节点纵向（默认/V） | 2×1 |
| 2 节点横向（H） | 1×2 |
| 横纵都有（V/H 链交错） | X×Y |

- 后续节点按其**自身 vh** 相对上一节点排布；首节点恒居格内原点 (0,0)
- 多个节点 = **增补绘制格**（不挤压）

## ③ 整列/整行统一扩展（XY 矩阵规范）

- **列宽 = 该列最大 nx ×（该列节点最大宽 + 2×half）**——该列所有交叉格按 nx_max 等分
- **行高 = 该行最大 ny ×（该行节点最大高 + 2×half）**——该行所有交叉格按 ny_max 等分
- 单节点格在宽列内占 1 个绘制格（居中），其余留空；**整条泳道统一扩展**（某格 X×Y → 所在整行/整列同步加绘制格单位）
- 空列/空行用基础格（BASE_W=100 / BASE_H=60），不盲目拉大

## ④ W / H 确定

- W = 该列中心节点最大宽；H = 该行中心节点最大高
- 节点位置按"绘制格宽 pw/高 ph"居中，不按列最大 W 强行拉大
- 基础尺寸 `NODE_BASE`：task 120×44、subprocess 140×48、gateway 80×50、start/end 圆 r=22、annotation 120×36、data 100×36；文字自适应不低于基础

## ⑤ 连线：两趟端口分配 + 最短路径

1. **第一趟（入口）**：每个节点的入口端口按朝向源的最短方向选，`usedIn` 记录
2. **第二趟（出口）**：出口端口优先选"未被出入口占"的方向；全占用则复用最短朝向端口
   - 规则：**出入口可各自共用（可共用入口/共用出口），但出入口之间不共用同一侧**
3. 走线正交：首段沿源端口法向（R/L 先横、T/B 先竖），末段沿目标端口法向；异行异列 = L 型一次拐弯，**去零长段与共线中间点**（不产生 U/n 形）
4. 条件分支**文本标签放在折线最长线段中点**（水平段在线上方、垂直段在线右侧）

## ⑥ 避障连线（确定性）

- 节点形状包围盒碰撞检测（Liang-Barsky 线段裁剪）
- 确定性避障顺序：**同行下移 → 同列右移 → 反向 → 扩格**，上限 3 轮防死循环
- 箭头抵目标节点边中点，不超出

## ⑦ 表头（格子化 + 默认居中）

- **流程图标题**：顶部通栏格子，宽度与绘制区 gridRight 对齐（无出血），居中
- **列标签（上表头）**：每列一格，泳道值居中；`AxisX` 标题在左上角格（水平）
- **行标签（左表头）**：每行一格，泳道值**旋转 -90°** 竖向排列；`AxisY` 标题在左上角格（水平）
- 单维泳道/无泳道 ROOT 占位不显示标签

## ⑧ 画布与整理布局

- SVG 内部已渲染完整标题/表头 → **组件层不重复渲染标题**，不额外添加布局控件
- **整理布局（tidyLayout）**：App.tsx 的「整理布局」按钮调用 `diagramRef.tidyLayout()`，将整体绘制内容**等比缩放至"尽可能占据画布"**（contain 适配容器，居中显示）
- **拖动平移**：`preventDefault` + `user-select:none` + `touch-action:none` + 阻止原生 drag；**平移软边界**（clampView 限制 tx/ty 不滑出内容太多，留 10% 余量）——避免拖出画布边缘导致页面空白
- pan/zoom（滚轮缩放以中心为锚、拖拽平移）

---

## 实现核对

- `computeExcelLayout`：ROOT 占位 + 单维泳道节点各占格 + cellXY(nx/ny V/H链) + 整列/整行统一扩展 + nodePos（节点居中）+ gridRight/gridBottom 对齐
- `flowToSVG`：网格矩形 + 虚线细泳道线（stroke-dasharray 4 4）+ 表头格子化（标题/列头/行头旋转）+ 两趟端口分配 + Liang-Barsky 避障 + 边标签最长线段中点 + 圆形节点字符超宽灰色差异底板
- `FlowDiagram`：fitView（宽/高优先切换）+ 整理布局按钮 + 拖动防白屏
- 断言：parser 40 项 + svg 33 项

---

*IQS-Flow 布局算法 v5（与代码持续同步）*
